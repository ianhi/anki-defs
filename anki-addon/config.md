## anki-defs Add-on Settings

Add-on-specific settings only. All other settings (AI provider, API keys, deck,
model, field mapping, etc.) are configured through the web UI at
`http://localhost:<port>` after starting the add-on.

- **port**: HTTP server port (default: 28735)
